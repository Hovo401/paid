# URL backend-API, который ВЫЗЫВАЕТ БРАУЗЕР пользователя (не контейнер).
# Должен быть доступен из браузера => используем хост-адрес, а не имя docker-сервиса.
#
#   локально / docker-compose : http://localhost:3000
#   прод (Render и т.п.)      : https://paid2-7uhm.onrender.com
#
# Префикс VITE_ обязателен — только такие переменные Vite отдаёт в код фронта.
VITE_API_URL=http://localhost:3000
